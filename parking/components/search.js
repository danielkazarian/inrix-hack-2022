import React, {useRef} from "react";

const Search = ({geocoderContainerRef}) => {
    // const search = new MapboxSearchBox();

    return (
        <div ref={geocoderContainerRef} className="flex w-[22rem] h-[4em] border-2 border-black">
            
        </div>
    )
}

export default Search